export default () => {
    return (
        <main>
            <h1>Welcome</h1>
            <p>
                This is an experimental script-writing application
                that stores the script in the browser, as well as
                allowing it to be stored to, and loaded from, file.
            </p>
            <p>
                Drag items to re-order or move them.
            </p>
        </main>
    );
}